apiVersion: v1
kind: Service
metadata:
  name: sap-orders
  labels:
    app: sap-orders
spec:
  type: ClusterIP
  selector:
    app: sap-orders
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: http
